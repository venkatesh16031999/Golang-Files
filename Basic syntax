package main;

import ( 
	"fmt"
	"strconv"
 );

var (
name1 string="hi"
name2 string="bye"
name3 string="see you"
);

func main(){
	var myName string = "venkatesh";
	fmt.Println(myName);
	number := 10;
	fmt.Println(name1);
	fmt.Println(strconv.Itoa(number));
}
